// if (typeof window === 'undefined') {
//   const { server } = require('./server')
//   server.listen()
//   console.info('🔶 Mock server running')
// } else {
//   const { worker } = require('./browser')
//   worker.start()
//   console.info('🔶 Mock worker running')
// }
